# req-145: Minificação de JS/CSS próprios — em build, não no roteamento

- **Status do intake**: ready-for-intake
- **Data**: 2026-08-29
- **Origem**: sugestão do Engenheiro Chefe durante o BATCH-146 ("sempre que um JS for roteado no
  nosso sistema pensei em minificar", apontando `controladores/arquivo-estatico/arquivo-estatico.php`)
- **Classificação**: desempenho de entrega, arquitetura de derivados
- **Batch candidato**: a definir

---

## 1. Oportunidade medida

**89 arquivos JS próprios, 2,7 MB não minificados** (excluindo `assets/vendor/`, que já vem
minificado do registro).

| Arquivo | Tamanho |
| --- | --- |
| `assets/datatables/datatables.js` | 485 KB |
| `assets/datatables/DataTables-1.10.23/js/jquery.dataTables.js` | 440 KB |
| `assets/interface/html-editor.js` | 345 KB |
| `assets/interface/html-editor-interface.js` | 130 KB |
| `modulos/dashboard/dashboard.toolbar.js` | 91 KB |
| `modulos/menus/menus.js` | 69 KB |

Os dois primeiros são **terceiros vendorizados à mão** em `assets/` — candidatos ao registro de
`assets-externos.php`, onde já existiria versão minificada oficial. Isso sozinho são 925 KB.

## 2. Por que NÃO minificar no `arquivo-estatico.php`

O controlador é a camada de **entrega**, e transformar o corpo ali colide com quatro coisas que ele
já faz corretamente (BATCH-100):

1. **`Content-Length` e `Range`**: o controlador anuncia `Accept-Ranges: bytes` e serve requisições
   parciais. Um corpo transformado em tempo de resposta não tem tamanho conhecido antes de ser
   gerado, e não há como servir um intervalo de bytes de algo que ainda não existe.
2. **`ETag` e `304`**: o ETag é derivado de `filesize` + `filemtime` do arquivo em disco. Minificar
   depois disso faz o validador descrever um corpo que não é o entregue.
3. **Custo por requisição**: sem cache, cada visitante paga a minificação. Com cache, é preciso
   inventar invalidação, diretório de cache e limpeza — reconstruindo um passo de build dentro do
   servidor de arquivos.
4. **Risco de quebra silenciosa**: minificador ingênuo que remove quebras de linha quebra código que
   depende de ASI (inserção automática de ponto e vírgula). O sintoma aparece em produção, no
   navegador do visitante.

## 3. Onde ela pertence

No **build**, seguindo a doutrina já estabelecida no req-141 / CR-002: `html` e `css` de autoria são
uma coisa; derivados são outra, sempre recalculáveis e nunca editados à mão.

O arquivo minificado é um **derivado** do JS de autoria, exatamente como `css_precompiled` é derivado
do `css`. Isso dá, de graça, o vocabulário que já existe:

- gerado por um estágio do pipeline (`resources:sync` ou um `c2f assets:build`), nunca em runtime;
- assinatura de procedência análoga ao `css_source_hash`, para saber quando o derivado deixou de
  corresponder à autoria;
- `arquivo-estatico.php` continua servindo **arquivo em disco**, com `Range`, `ETag` e `304`
  intactos — ele só passa a receber um arquivo menor.

## 4. Esboço de escopo

1. Migrar DataTables para `assets-externos.php` (elimina 925 KB de terceiros não minificados).
2. Estágio de build que gera `<id>.min.js` ao lado do fonte, com procedência registrada.
3. Emissor (`gestor_pagina_javascript_incluir`) preferindo o minificado quando ele existe **e** a
   procedência confere; caindo no fonte quando não.
4. Gate por ambiente: em `DEVELOPMENT_ENV=true` servir sempre o fonte, para não depurar código
   minificado.
5. Teste estrutural garantindo que nenhum minificado seja versionado sem procedência válida.

## 5. Recomendação

Vale fazer, e o item 1 sozinho já entrega quase um terço do ganho sem escrever minificador nenhum.
Mas **não** no `arquivo-estatico.php`: minificar na entrega troca um problema de tamanho por quatro
problemas de correção, e joga fora garantias de HTTP que o controlador levou um lote para ganhar.
