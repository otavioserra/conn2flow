# Requisição 182 — Documentação do Core: Guias, Conceitos, CLI, API, Novidades e Publicação do SDD (FEAT-014, fase 2, ondas 4 a 6)

**Origem**: Humano, 2026-09-26: os créditos do agente da req-179 estão acabando; dividir o que falta com outro agente, em outra infraestrutura.
**Lote Vinculado**: `BATCH-186`.
**Status**: `approved`.
**Autonomia**: Nível 2. Commit e push autorizados pelo Humano. Deploy **só no Lab local** (`conn2flow-site-local`, `local: true`). **Nunca produção**: o Humano publica depois de revisar.
**Leitura obrigatória antes de começar**:
- skill `c2f-documentation`;
- [req-179.md](req-179.md): regras (§1), mapa legado → árvore (§2.3, §2.4, §2.5) e ondas (§3);
- `ai-workspace/pt-br/docs/guides/documentation.md` (o contrato, inclusive a chave `label`);
- [BATCH-184.md](../implementation/BATCH-184.md): achados já registrados, para não repetir o trabalho;
- exemplos prontos do nível de rigor esperado: `concepts/resources.md`, `concepts/hooks.md`, `reference/libraries/cron.md`, `reference/libraries/seguranca.md`.

---

## 1. Premissa (Humano)

As docs legadas foram escritas por uma geração anterior de agentes, que **errava muito**. Parta do princípio de que **toda doc legada está errada** e refaça a partir do código. Use os comentários do código (req-xxx, DEC-xxx), mas confirme cada afirmação lendo a implementação; quando possível, rode o código (`php -r`, `c2f <comando> --help`). Há muita coisa implementada sem documentação, e ela também deve entrar.

## 2. Estado na entrega (2026-09-26)

- Feito: onda 1 (conceitos `request-lifecycle`, `global-variables`, `resources`, `hooks`), onda 2 (39 de 41 bibliotecas; `formulario` e `html-editor` ficam com o agente da req-179), onda 3 (32 módulos, req-180), `guides/installation.md` e `guides/documentation.md`.
- `docs:audit`: 0 erros. Restam 35 docs legadas por idioma na raiz de `ai-workspace/{pt-br,en}/docs/` e nas pastas `visao/`, `changelogs/`, `bibliotecas/` (só `README.md`/`ROADMAP.md`) e `libraries/`.

## 3. Escopo

| Onda | Docs novas (pt-br e en, mesmo caminho) | Legado a ler (pista, não fonte) e apagar ao migrar |
|---|---|---|
| 4 — guias | `guides/development-environment.md` (inclui Docker e Cloudflare Tunnel), `guides/dev-tools-vscode.md`, `guides/ci-cd.md`, `guides/create-a-module.md`, `guides/create-a-plugin.md`, `guides/deploy-a-project.md` | `CONN2FLOW-AMBIENTE-DESENVOLVIMENTO`, `CONN2FLOW-AMBIENTE-DOCKER`, `CONN2FLOW-CLOUDFLARE-TUNNEL`, `GUIA-PAINEL-DEV-TOOLS-VSCODE`, `CONN2FLOW-GITHUB-ACTIONS`, `CONN2FLOW-PLUGIN-INSTALADOR-FLUXO`, `CONN2FLOW-AUTOMACAO-EXPORTACAO`, `CONN2FLOW-HISTORICO-EXPORTACAO`, en `PHP85-INSTALL-GUIDE` (+ equivalentes en) |
| 4 — referência | `reference/cli/index.md` + uma página por grupo de comandos (`cli/src/Commands/`, 52 arquivos; o `--help` de cada comando é o ponto de partida, e o código a fonte), `reference/api/index.md` (+ páginas por grupo de endpoints de `gestor/controladores/api/`) | — (doc nova) |
| 5 — conceitos | `concepts/architecture.md`, `widgets.md`, `multilingual.md`, `plugins.md`, `projects.md`, `css-and-tailwind.md`, `admin-interface.md`, `system-updates.md`, `ai-security.md`, `vision.md` | `CONN2FLOW-GESTOR-DETALHAMENTO`, `CONN2FLOW-MODULOS-DETALHADO`, `CONN2FLOW-MODULOS-OVERVIEW`, `CONN2FLOW-SISTEMA-HIBRIDO-MULTILANGUE-CONCLUIDO`, `CONN2FLOW-SISTEMA-MULTILÍNGUE-HIBRIDO`, `CONN2FLOW-PLUGIN-ARCHITECTURE`, `CONN2FLOW-SISTEMA-PROJETOS`, `CONN2FLOW-PROTECAO-BANCO-PROJETOS`, `CONN2FLOW-FRAMEWORK-CSS`, `CONN2FLOW-SISTEMA-PREVIEW-MODALS`, `CONN2FLOW-MENU-RESPONSIVO-DASHBOARD-I18N-v2.6.3`, `CONN2FLOW-ATUALIZACOES-SISTEMA`, `estrategia-seguranca-ia-publica`, `CONN2FLOW-SISTEMA-CONHECIMENTO`, `visao/*` (+ equivalentes en). O mapa exato de cada um está na req-179 §2.3 |
| 5 — novidades | `whats-new/index.md` e uma página por versão relevante, a partir do `CHANGELOG.md` da raiz | `CONN2FLOW-CHANGELOG-HISTORY`, `changelogs/*`, o `README.md` da raiz de `docs/` (req-179 §2.5) |
| 5 — limpeza | Quando o acervo inteiro estiver migrado: apagar `bibliotecas/README.md`, `bibliotecas/ROADMAP.md`, `libraries/README.md`, `libraries/ROADMAP.md` e revisar `ai-workspace/{pt-br,en}/README.md` | — |
| 6 — SDD | Publicar o `sdd/` **do core** como mais um publisher do site (`docs-sdd`): modo de fonte "SDD" no `c2f docs:build`, com filtro de conteúdo sensível (hosts, IPs, credenciais, caminhos de servidor) e testes PHPUnit. Nunca o SDD de projetos privados | — |

Achados já conhecidos que devem entrar nas docs novas (detalhes no BATCH-184): o deploy não atualiza o `sitemap.xml` (conceito `projects`/guia `deploy-a-project`); nada cria o agendamento do cron no servidor (guia `deploy-a-project`); só SMTPS/porta 465 funciona no e-mail; os achados de segurança A1–A11 estão na [req-181](req-181.md) (conceito `ai-security` e os guias devem apontar para ela, sem repetir detalhes de exploração).

## 4. Regras de convivência (obrigatórias)

1. **Só edite** `ai-workspace/{pt-br,en}/docs/{guides,concepts,whats-new}/` (exceto `guides/installation.md`, `guides/documentation.md` e os quatro conceitos da onda 1, salvo correção de link), `reference/cli/`, `reference/api/`, os legados listados em §3 e `sdd/implementation/BATCH-186.md`. `reference/libraries/` e `reference/modules/` não são seus.
2. **Pipeline:** não rode `c2f docs:build`, `project:update-all`, `css:rebuild` nem `resources:sync` enquanto o `BATCH-184` não estiver `complete` no `BATCH-INDEX.md`. Depois disso a publicação no Lab passa a ser sua: `php cli/c2f.php docs:build --project=conn2flow-site-local` e `php cli/c2f.php project:update-all conn2flow-site-local`, **um de cada vez, em primeiro plano**, e commit dos recursos gerados no repositório `conn2flow-site` (branch `feat/req-055`).
3. **Pode rodar** `php cli/c2f.php docs:audit` e `docs:extract` a qualquer momento.
4. **Git:** `git fetch` e confira `git rev-list --count HEAD..origin/main` antes de cada push; `git add` só com os seus caminhos (nunca `-A` nem `.`); commits pequenos com a mensagem `docs(core): <escopo> (BATCH-186 / req-182)` e a linha `Co-Authored-By`; push após cada commit.
5. **Heredoc do Git Bash corrompe barras invertidas e aspas.** Escreva e edite arquivos com as ferramentas de arquivo (Write/Edit), ou com um script salvo em arquivo.
6. **Links:** só para `.md` que já existam; link quebrado aborta o build. Não use um link "provisório" para outra página.
7. **Conteúdo público:** nada de hosts, IPs, credenciais ou caminhos de servidor reais.

## 5. Critério de aceite

1. `docs:audit --json`: 0 erros, nenhum item de guides/concepts/reference/whats-new com score > 0 e `legacy` = 0 nos dois idiomas.
2. Onda 6: publisher `docs-sdd` gerado pelo `docs:build`, com testes do filtro de conteúdo sensível passando.
3. Site no Lab republicado e conferido com `c2f page:inspect` (0 erros de console) nas páginas novas.
4. `BATCH-186.md` com as docs, os achados do código por doc e os hashes dos commits.
5. Ao final, uma lista de testes manuais para o Humano (o que abrir e o que conferir no Lab).
