# Requisição 176 — Snapshot e Limpeza do Legado do `ai-workspace` (FEAT-014, fase 0)

**Origem**: Programa de Documentação [FEAT-014](file:///c:/Users/otavi/OneDrive/Documentos/GIT/conn2flow-ai-workspace/sdd/backlog/FEAT-014-programa-documentacao-core-e-site.md), decisão D4 do Humano (2026-09-25).
**Lote Vinculado**: `BATCH-181`.
**Status**: `approved`.
**Autonomia**: Nível 1 (supervisionado): sem commit nem push pelo agente.

---

## 1. Contexto

`ai-workspace/{pt-br,en}/` acumula material do fluxo anterior ao SDD, que não é mais usado:
- `agents-history/` (conversas antigas de agentes);
- `prompts/` (prompts do fluxo antigo);
- `templates/` (modelos de implementação pré-SDD).

Isso polui o contexto dos agentes e o futuro `docs:audit`.

## 2. Escopo

1. **Snapshot:** criar a branch local `legacy/ai-workspace-pre-docs` apontando para o `HEAD` atual da `main`, preservando o estado integral antes da limpeza. O push fica com o operador.
2. **Remoção na `main` (working tree):** `ai-workspace/{pt-br,en}/agents-history/`, `ai-workspace/{pt-br,en}/prompts/` e `ai-workspace/{pt-br,en}/templates/`.
3. **Fora do escopo (proibido remover):**
   - `ai-workspace/{pt-br,en}/scripts/`: código vivo, com mais de 80 referências a partir de `cli/`, `.vscode` e afins;
   - `docs/`, `snippets/`, `utils/` (avaliados na req-177).
4. Atualizar `ai-workspace/README.md` e `ai-workspace/{pt-br,en}/README.md`, retirando os links das pastas removidas e apontando a branch de snapshot.
5. Nenhuma referência viva pode ficar apontando para as pastas removidas: fazer grep em `cli/`, `gestor/`, `.vscode/`, `.github/`, `dev-environment/`, `tests/`, `package.json` e skills.

## 3. Critérios de Aceite

1. A branch `legacy/ai-workspace-pre-docs` existe e contém as três pastas.
2. As três pastas não existem mais na working tree da `main`.
3. O grep de referências vivas às pastas removidas retorna vazio (ou só histórico em `sdd/`).
4. A suíte PHPUnit não regride.
